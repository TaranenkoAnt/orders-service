Subject: [PATCH] 8_kafka_docker_2
---
Index: docker/docker-compose.yml
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/docker/docker-compose.yml b/docker/docker-compose.yml
--- a/docker/docker-compose.yml	(revision 84badc67971af90010f3d7f350a5ec1e17d9c4bb)
+++ b/docker/docker-compose.yml	(date 1730822088087)
@@ -75,6 +75,90 @@
       -c max_wal_senders=1
       -c max_replication_slots=1
 
+  kafka:
+    image: confluentinc/cp-kafka:7.5.2
+    hostname: kafka
+    container_name: kafka
+    ports:
+      - "9097:9092"
+    environment:
+      KAFKA_BROKER_ID: 1
+      KAFKA_NODE_ID: 1
+      KAFKA_PROCESS_ROLES: broker,controller
+      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
+      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT'
+      KAFKA_ADVERTISED_LISTENERS: ${KAFKA_INTERNAL_ADVERTISED_LISTENER},${KAFKA_EXTERNAL_ADVERTISED_LISTENER}
+      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
+      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
+      KAFKA_LISTENERS: ${KAFKA_INTERNAL_ADVERTISED_LISTENER},${KAFKA_CONTROLLER_LISTENER},${KAFKA_PLAINTEXT_LISTENER}
+      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
+      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
+      KAFKA_CONFLUENT_SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL}
+      KAFKA_LOG_DIRS: '/tmp/kraft-combined-logs'
+      KAFKA_AUTO_CREATE_TOPICS_ENABLE: false
+      CLUSTER_ID: X1oISRwqQ1yqeRlIjsegqQ
+    volumes:
+      - kafka-data:/var/lib/kafka/data
+
+  schema-registry:
+    image: confluentinc/cp-schema-registry:7.5.2
+    hostname: schema-registry
+    container_name: schema-registry
+    depends_on:
+      - kafka
+    ports:
+      - "8081:8081"
+    environment:
+      SCHEMA_REGISTRY_HOST_NAME: schema-registry
+      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
+      SCHEMA_REGISTRY_LISTENERS: ${SCHEMA_REGISTRY_LISTENERS}
+
+  connect:
+    image: confluentinc/cp-kafka-connect:7.5.2
+    container_name: connect
+    ports:
+      - "8083:8083"
+    depends_on:
+      kafka:
+        condition: service_started
+      orders-service:
+        condition: service_started
+    environment:
+      CONNECT_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
+      CONNECT_GROUP_ID: "debezium-cloudjava"
+      CONNECT_REST_ADVERTISED_HOST_NAME: connect
+      CONNECT_TOPIC_CREATION_ENABLE: "true"
+
+      CONNECT_CONFIG_STORAGE_TOPIC: connect-configs
+      CONNECT_OFFSET_STORAGE_TOPIC: connect-offsets
+      CONNECT_STATUS_STORAGE_TOPIC: connect-statuses
+
+      CONNECT_REPLICATION_FACTOR: 1
+      CONNECT_CONFIG_STORAGE_REPLICATION_FACTOR: 1
+      CONNECT_OFFSET_STORAGE_REPLICATION_FACTOR: 1
+      CONNECT_STATUS_STORAGE_REPLICATION_FACTOR: 1
+
+      CONNECT_KEY_CONVERTER: org.apache.kafka.connect.storage.StringConverter
+      CONNECT_VALUE_CONVERTER: io.confluent.connect.avro.AvroConverter
+      CONNECT_VALUE_CONVERTER_SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL}
+      # CLASSPATH required due to CC-2422
+      CLASSPATH: "/usr/share/java/monitoring-interceptors/monitoring-interceptors-7.6.0.jar"
+      CONNECT_PLUGIN_PATH: '/usr/share/java,/usr/share/confluent-hub-components/,/connectors/'
+    volumes:
+      - connect-data:/opt/docker/db/data
+    command:
+      - bash
+      - -c
+      - |
+        echo "Installing connector plugins"
+        confluent-hub install --no-prompt debezium/debezium-connector-postgresql:2.5.3
+        echo "Launching Kafka Connect worker"
+        /etc/confluent/docker/run
+
 volumes:
   db-data:
+    driver: local
+  kafka-data:
+    driver: local
+  connect-data:
     driver: local
\ No newline at end of file
Index: docker/.env
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/docker/.env b/docker/.env
--- a/docker/.env	(revision 84badc67971af90010f3d7f350a5ec1e17d9c4bb)
+++ b/docker/.env	(date 1729794331914)
@@ -7,3 +7,12 @@
 MENU_DB_URL=jdbc:postgresql://postgres:5432/menu_service_db
 ORDERS_DB_URL=r2dbc:postgresql://postgres:5432/orders_service_db
 ORDERS_DB_FLYWAY_URL=jdbc:postgresql://postgres:5432/orders_service_db
+
+KAFKA_INTERNAL_ADVERTISED_LISTENER=PLAINTEXT://kafka:29092
+KAFKA_EXTERNAL_ADVERTISED_LISTENER=PLAINTEXT_HOST://localhost:9097
+KAFKA_PLAINTEXT_LISTENER=PLAINTEXT_HOST://0.0.0.0:9092
+KAFKA_CONTROLLER_LISTENER=CONTROLLER://kafka:29093
+KAFKA_BOOTSTRAP_SERVERS=kafka:29092
+
+SCHEMA_REGISTRY_URL=http://schema-registry:8081
+SCHEMA_REGISTRY_LISTENERS=http://0.0.0.0:8081
